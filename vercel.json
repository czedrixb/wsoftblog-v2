{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && DATABASE_URI=\"${DATABASE_URI_SESSION:-$DATABASE_URI}\" corepack pnpm payload migrate && corepack pnpm build"
}
